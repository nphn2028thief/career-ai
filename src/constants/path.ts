enum EPath {
  HOME = "/",
  DASHBOARD = "/dashboard",
  RESUME = "/resume",
  COVER_LETTER = "/ai-cover-letter",
  INTERVIEW_PREP = "/interview-prep",
  ONBOARDING = "/onboarding",
}

export default EPath;
